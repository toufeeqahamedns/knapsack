name: knapsack
description: A Flutter Project to demonstrate knapsack problem with charts.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^7.0.1
  equatable: ^2.0.3

  fl_chart: ^0.36.2
  random_color: ^1.0.6-nullsafety
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true